Chrome浏览器中添加React开发者工具扩展, 浏览器F12 console控制台中新增Components标签页，可以直接检查各个组件在当前页面上的布局信息。 

首次启动和运行应用：
npm create vite@latest my-app -- --template react
cd my-app
npm install        # 安装依赖（第一次运行时）
npm run dev -- --port 5173       # 启动开发服务器

之前已经启动过了：
npm run dev -- --port 5173